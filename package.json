{
  "name": "whoop-mcp-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  },
  "keywords": ["whoop", "mcp", "health", "fitness", "cloudflare-workers"],
  "author": "Conor Cavanaugh",
  "license": "MIT",
  "description": "WHOOP MCP server for Claude — access recovery, sleep, strain, and workout data",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "agents": "^0.9.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250327.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
